insert into TESTDB.AvgCiteB

SELECT A.ToPaperID, AVG(B.CNT) AS MEAN
from TESTDB.AUTHOR_CITE AS A
INNER JOIN (
		select OLD_AUTHOR_ID, COUNT(*) AS CNT
		FROM TESTDB.AUTHOR_CITE_COUNT
		GROUP BY OLD_AUTHOR_ID)  AS B
ON A.OLD_AUTHOR_ID=B.OLD_AUTHOR_ID
GROUP BY A.ToPaperID
