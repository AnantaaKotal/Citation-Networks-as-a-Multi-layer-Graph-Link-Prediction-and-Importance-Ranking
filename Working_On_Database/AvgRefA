insert into TESTDB.AvgRefA

SELECT A.FromPaperID, AVG(B.CNT) AS MEAN
from TESTDB.AUTHOR_CITE AS A
INNER JOIN (
		select NEW_AUTHOR_ID, COUNT(*) AS CNT
		FROM TESTDB.AUTHOR_CITE_COUNT
		GROUP BY NEW_AUTHOR_ID)  AS B
ON A.NEW_AUTHOR_ID=B.NEW_AUTHOR_ID
GROUP BY A.FromPaperID
